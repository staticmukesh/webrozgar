@model WebRozgar.ViewModels.ProfileViewModel
<p>Profile Updated successfully!!!!</p>
@if ((Model.IsProfileCompleted == -1)&&(User.IsInRole("seeker")))
{
    @Html.ActionLink("Continue Step 2 of 2", "Manage", new { id="skills" })
}